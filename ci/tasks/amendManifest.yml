---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
  - name: quotes-service
  - name: resource-version

outputs:
  - name: output

run:
  path: "quotes-service/ci/scripts/amendManifest.sh"
