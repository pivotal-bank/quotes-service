---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: governmentpaas/cf-cli

inputs:
  - name: quotes-service

run:
  path: "quotes-service/ci/scripts/createServiceRegistry.sh"
